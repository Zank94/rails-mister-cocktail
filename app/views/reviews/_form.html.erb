<%= simple_form_for [@cocktail, @review] do |f| %>
  <%= f.input :rating, collection: 0..5, prompt: "Choose a note between 0 and 5" %>
  <%= f.input :content %>
  <%= f.button :submit, class: "btn btn-primary btn-lg" %>
<% end %>
